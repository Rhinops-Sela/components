
{
  "name": "email-alert",
  "email_configs": [
    {
      "to": "${TO}",
      "from": "${FROM}",
      "smarthost": "${SMTP_SERVER}:587",
      "auth_username": "${USERNAME}",
      "auth_identity": "${FROM}",
      "auth_password": "${PASSWORD}"
    }
  ]
}